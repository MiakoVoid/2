<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 数字按钮样式 -->
    <style name="NumberButtonStyle" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="backgroundTint">@color/white</item>
        <item name="cornerRadius">8dp</item>
        <item name="rippleColor">@color/primary_color_ripple</item>
    </style>

    <!-- 数字操作按钮样式 -->
    <style name="NumberOperatorButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/primary_color</item>
        <item name="cornerRadius">8dp</item>
        <item name="rippleColor">@color/primary_color_ripple</item>
    </style>

    <!-- 数字特殊按钮样式 -->
    <style name="NumberSpecialButtonStyle" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="backgroundTint">@color/surface_secondary</item>
        <item name="cornerRadius">8dp</item>
        <item name="rippleColor">@color/primary_color_ripple</item>
    </style>

</resources>